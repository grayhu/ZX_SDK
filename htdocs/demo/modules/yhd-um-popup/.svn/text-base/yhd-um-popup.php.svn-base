<?php
/**
 内容规则：
 1.PHP页面内容只能包含一个根元素（允许任意标签元素，推荐"div"元素）
 2.根元素类定义包含：class="yhd-um-popup"（class属性可以添加您需要的类选择器定义）
 3.禁止使用<style>标签（元素）
 4.禁止使用<script>标签（元素）
 5.禁止使用<link>标签（元素）
 6.禁止使用标签（元素）的id属性
 7.允许使用元素内联style属性
 */
?>
<div class="yhd-um-popup">
<?php
/**
 * 开始设计PHP页面
 */
?>
	<div class="main">
		<div class="first-trigger">我只是个触点而已，把鼠标滑到我身上
			<div class="J_TWidget hidden" data-widget-type="Popup" data-widget-config="{
				  'trigger':'.first-trigger',
				  'align':{
						  'node':'.first-trigger',
						  'offset':[0,0],
						  'points':['cc','cc']
						  }
					}">
				<div style="background-color: blue; height: 100px; width: 200px;">
					我是一个弹出层
				</div>
			</div>
		</div>
	</div>
</div>
